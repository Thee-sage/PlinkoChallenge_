# Production Environment Variables for Free Hosting
# Copy these to your hosting platform's environment variables section

# MongoDB Configuration (MongoDB Atlas Free Tier)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/plinkochallenge?retryWrites=true&w=majority

# Server Configuration
PORT=3001
NODE_ENV=production

# URLs - UPDATE THESE WITH YOUR ACTUAL DEPLOYED URLS
BASE_URL=https://your-backend.railway.app
FRONTEND_URL=https://your-frontend.vercel.app

# CORS - Add your frontend URL here (comma-separated for multiple)
ALLOWED_ORIGINS=https://your-frontend.vercel.app,https://your-custom-domain.com

# JWT Secret - Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-generated-secret-key-here

# Google OAuth Configuration
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Gmail SMTP Configuration (Free)
# IMPORTANT: You need to create a Gmail App Password (not your regular password)
# Steps: Google Account → Security → 2-Step Verification → App Passwords
# The app password will be 16 characters long
GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=your-gmail-app-password-16-characters

# Admin Configuration
# Generate hash with: node -e "const bcrypt=require('bcrypt');bcrypt.hash('your-password',10).then(h=>console.log(h))"
ADMIN_APPROVAL_PASSWORD_HASH=your-bcrypt-hashed-password-here

